// Write a c program to print no from 0 to 9 by the help of do while loop

#include <stdio.h>

int main()
{
    int i=0;
    do
    {
        printf("%d ",i);
        i++;
    }
    while(i<10);
    return 0;
}
