// Write a c program to print no from 0 to 9 by the help of while loop

#include <stdio.h>

int main()
{
    int i=0;
    while(i<10)
    {
        printf("%d ",i);
        i++;
    }

    return 0;
}
